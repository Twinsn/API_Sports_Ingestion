services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sport_ingestion
      POSTGRES_PASSWORD: sport_ingestion
      POSTGRES_DB: sport_ingestion
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
